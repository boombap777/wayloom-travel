# Tested local experiment environment. Install a CUDA-compatible torch build first when needed.
torch==2.11.0+cu128
transformers==5.14.1
peft==0.18.1
bitsandbytes==0.50.2
accelerate==1.14.0
safetensors>=0.5.0

